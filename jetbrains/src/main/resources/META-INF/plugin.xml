<idea-plugin>
  <id>com.chestnutch.cursor-ime-hud</id>
  <name>Cursor IME HUD</name>
  <version>0.1.1</version>
  <vendor email="support@chestnut-ch.dev" url="https://github.com/GJYNBB/cursor-ime-hud">chestnut-ch</vendor>
  <description><![CDATA[Cursor IME HUD shows the current Chinese/English input state in JetBrains IDEs.
    <p>It displays the state in the status bar and beside the editor caret, uses platform-native Rust helpers on Windows, macOS, and Linux, and never reads typed text, clipboard data, or file contents.</p>
    <p>When a backend cannot determine the state reliably, the plugin shows <code>unknown</code> and keeps diagnostics available.</p>
    <p><b>中文说明：</b>专为中文输入法用户设计，在状态栏和编辑器光标旁显示当前中文/英文输入状态，减少因输入法模式错误而产生的误输入。</p>
    <p>插件在 Windows、macOS 和 Linux 上通过本机 Rust 助手读取系统公开的输入法、输入源或键盘布局状态。它不会采集键入内容、剪贴板数据或文件内容，也不会主动切换系统输入法。</p>
    <p>当当前平台无法可靠判断输入状态时，插件会显示<code>未知</code>，并保留诊断信息以便排查。</p>
  ]]></description>
  <change-notes><![CDATA[
    <ul>
      <li>0.1.1：Helper 进程与快照状态改为应用级共享，多项目只启动一个助手进程；快照状态迁移改为无锁原子更新。</li>
      <li>0.1.1：助手可执行文件改为按哈希缓存复用，不再每次启动重新解压临时副本。</li>
      <li>0.1.1：启动活动与设置页迁移到官方公开 API（ProjectActivity / 状态栏公开刷新），界面文案统一走资源包。</li>
      <li>首公开版本：在状态栏与光标旁显示中文/英文输入法状态，覆盖 Windows / macOS / Linux。</li>
      <li>修复 Helper 在 STOPPING / STARTING 交叉时的卡死与孤儿进程问题。</li>
      <li>修复意图预览期间更新 HUD 触发的副作用异常，以及折叠展开后光标旁位置不同步。</li>
      <li>状态栏悬停与点击菜单改用简短中文提示；点击菜单可开启/关闭光标旁图标，并保留刷新 / 诊断 / 设置；设置页偏移范围统一为 -50 ~ 50。</li>
    </ul>
  ]]></change-notes>
  <idea-version since-build="261"/>

  <depends>com.intellij.modules.platform</depends>
  <depends>com.intellij.modules.lang</depends>
  <resource-bundle>messages.CursorImeHudBundle</resource-bundle>

  <extensions defaultExtensionNs="com.intellij">
    <postStartupActivity implementation="com.chestnutch.cursorimehud.ui.caret.CaretHudStartupActivity"/>
    <statusBarWidgetFactory id="CursorImeHudStatusBar" implementation="com.chestnutch.cursorimehud.ui.ImeStatusBarWidgetFactory"/>
    <applicationConfigurable instance="com.chestnutch.cursorimehud.settings.CursorImeHudConfigurable" id="cursorImeHud" key="settings.displayName" bundle="messages.CursorImeHudBundle"/>
  </extensions>

  <!-- Action text/description resolve to action.<id>.text / action.<id>.description in the bundle. -->
  <actions resource-bundle="messages.CursorImeHudBundle">
    <action id="CursorImeHud.RefreshImeState" class="com.chestnutch.cursorimehud.action.RefreshImeStateAction"/>
    <action id="CursorImeHud.ToggleCaretHud" class="com.chestnutch.cursorimehud.action.ToggleCaretHudAction"/>
    <action id="CursorImeHud.ShowDiagnostics" class="com.chestnutch.cursorimehud.action.ShowDiagnosticsAction"/>
  </actions>
</idea-plugin>
